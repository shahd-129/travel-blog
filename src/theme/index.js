// theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#000000ff",
    },
    secondary: {
      main: "#9c27b0",
    },
  },
});

export default theme;
